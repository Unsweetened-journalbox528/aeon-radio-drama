# aeon-radio-drama — Python deps.
# Runtime only. ComfyUI itself + the ACE/MMAudio/SAO/Qwen3 models live separately.

pedalboard>=0.9.0   # mastering chain (HPF, EQ, Distortion, Clipping)
librosa>=0.10.0     # LUFS / DR / crest measurement
soundfile>=0.12.0   # WAV/FLAC IO
numpy>=1.24
scipy>=1.10         # filter design
requests>=2.28      # ComfyUI HTTP API client
